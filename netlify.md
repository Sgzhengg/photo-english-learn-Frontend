# Netlify 部署指南

## 📦 推荐方式：拖拽 dist 文件夹（最快）

### 步骤 1: 本地构建

```bash
cd E:\photo-english-learn-Frontend
npm run build
```

构建完成后，会在 `dist` 文件夹生成生产文件。

### 步骤 2: 拖拽部署

1. 访问 [Netlify Drop](https://app.netlify.com/drop)
2. 将 `dist` 文件夹拖拽到页面中
3. 等待上传完成（约 1-2 分钟）
4. 完成！Netlify 会提供访问 URL

---

## 🔧 方式 2: 拖拽整个项目文件夹

1. 访问 [Netlify Drop](https://app.netlify.com/drop)
2. 将 `photo-english-learn-Frontend` 整个文件夹拖拽进去
3. Netlify 会自动检测：
   - Build command: `npm run build`
   - Publish directory: `dist`
4. 等待构建和部署完成

---

## ✅ 已配置的文件

- **`public/_redirects`**: SPA 路由支持（所有路由都返回 index.html）
- **`vite.config.ts`**: 已配置路由别名

---

## 🎯 部署后检查

部署完成后，访问 Netlify 提供的 URL，检查：

- ✅ 页面正常加载
- ✅ 路由切换正常（刷新页面不会 404）
- ✅ API 调用成功
- ✅ 图片上传功能正常
- ✅ 登录/注册功能正常

---

## 🔗 自定义域名（可选）

1. 在 Netlify 控制台点击 "Domain settings"
2. 添加自定义域名
3. 配置 DNS 记录
4. 等待 SSL 证书生成

---

## ⚠️ 注意事项

1. **API 地址**: 当前配置为 `https://photo-english-learn-api-gateway.zeabur.app`
   - 如需修改，编辑 `src/lib/api.ts`

2. **构建前检查**: 确保所有依赖已安装
   ```bash
   npm install
   ```

3. **本地测试构建**:
   ```bash
   npm run build
   npm run preview
   ```
   访问 `http://localhost:4173` 查看效果
